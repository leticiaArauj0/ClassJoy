import styled from "styled-components";

export const ContainerDiaryNotes = styled.div`
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;

    .container-notice {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
`
